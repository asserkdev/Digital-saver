name: digital_saver
description: Professional smartwatch health monitoring with advanced features
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  flutter_blue_plus: ^1.32.0
  permission_handler: ^11.3.0
  geolocator: ^12.0.0
  url_launcher: ^6.2.5
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  flutter_local_notifications: ^17.0.0
  http: ^1.2.0
  provider: ^6.1.1
  fl_chart: ^0.66.0
  shimmer: ^3.0.0
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  generate: true
